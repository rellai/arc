CREATE TABLE diagram (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   name VARCHAR(255),
   img CLOB,
   code CLOB,
   parent_id BIGINT,
   CONSTRAINT pk_diagram PRIMARY KEY (id)
);

ALTER TABLE diagram ADD CONSTRAINT FK_DIAGRAM_ON_PARENT FOREIGN KEY (parent_id) REFERENCES diagram (id);